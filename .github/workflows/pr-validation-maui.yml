name: eShop Pull Request Validation - .NET MAUI

# Disabled for this lab repo - the repo name (87 chars) causes Android AAPT2
# path length failures that cannot be fixed without renaming the repo.
# The MAUI ClientApp is not part of the lab exercises.
on:
  workflow_dispatch:

jobs:  
  test:
    runs-on: windows-latest    
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET (global.json)
        uses: actions/setup-dotnet@v3

      - name: Update Workloads
        run: dotnet workload update

      - name: Install Workloads
        shell: pwsh
        run: |
          dotnet workload install android
          dotnet workload install ios
          dotnet workload install maccatalyst
          dotnet workload install maui

      - name: Build 
        run: dotnet build src/ClientApp/ClientApp.csproj
        
      - name: Test
        run: dotnet test --project tests/ClientApp.UnitTests/ClientApp.UnitTests.csproj --no-progress --output detailed