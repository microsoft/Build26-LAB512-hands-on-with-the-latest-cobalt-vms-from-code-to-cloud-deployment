name: eShop Pull Request Validation - .NET MAUI

# Disabled for this lab repo - the repo name exceeds Windows 260-char path
# limits when combined with deep .cs file paths. Uncomment to re-enable.
on:
  workflow_dispatch:
#  pull_request:
#    branches:
#      - '**'
#    paths:
#      - 'src/ClientApp/**'
#      - 'tests/ClientApp.UnitTests/**'
#      - '.github/workflows/pr-validation-maui.yml'
#  push:
#    branches:
#      - main
#    paths:
#      - 'src/ClientApp/**'
#      - 'tests/ClientApp.UnitTests/**'
#      - '.github/workflows/pr-validation-maui.yml'

jobs:  
  test:
    runs-on: windows-latest    
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET (global.json)
        uses: actions/setup-dotnet@v3

      - name: Update Workloads
        run: dotnet workload update

      - name: Install Workloads
        shell: pwsh
        run: |
          dotnet workload install android
          dotnet workload install ios
          dotnet workload install maccatalyst
          dotnet workload install maui

      - name: Build 
        run: dotnet build src/ClientApp/ClientApp.csproj
        
      - name: Test
        run: dotnet test --project tests/ClientApp.UnitTests/ClientApp.UnitTests.csproj --no-progress --output detailed